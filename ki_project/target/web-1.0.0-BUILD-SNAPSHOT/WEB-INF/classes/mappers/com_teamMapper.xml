<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.kiosk.mapper.Com_teamMapper">

	<select id="read" resultType="Com_teamVO">
		select section_cd,team_cd
		from com_team
		where
		section_cd = #{section_cd} and team_cd = #{team_cd}
	</select>

	<insert id="create">
		insert into com_team
		(section_cd,team_cd,team_nm,team_sort,team_use)
		values
		(#{section_cd},#{team_cd},#{team_nm},#{team_sort},#{team_use})
	</insert>

	<update id="update">
		update com_team set
		team_nm = #{team_nm},
		team_sort = #{team_sort},
		team_use = #{team_use}
		where
		section_cd = #{section_cd} and team_cd = #{team_cd}
	</update>

	<delete id="delete">
		delete from com_team where section_cd = #{section_cd} and team_cd = #{team_cd}
	</delete>

	<select id="listAll" resultType="Com_teamVO">
 		<![CDATA[
 		select * 
 		from com_team
 		order by section_cd asc, team_sort asc, team_cd asc 
 		]]>
	</select>
	
	<select id="list" resultType="Com_teamVO">
 		<![CDATA[
 		select * 
 		from com_team
 		where section_cd =#{section_cd}
 		order by section_cd asc, team_sort asc, team_cd asc 
 		]]>
	</select>
	
	<select id="jsonList" resultType="Com_teamVO">
 		<![CDATA[
 		select * 
 		from com_team 
 		where section_cd like concat(#{bureau_cd},'%')
 		order by section_cd asc
 		]]>
	</select>
	
	<select id="lastInsertSort" resultType="int">
		select count(*)
		from com_team
		where section_cd = #{section_cd}
	</select>
	
</mapper>