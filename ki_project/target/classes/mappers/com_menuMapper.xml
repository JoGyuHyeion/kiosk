<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.kiosk.mapper.Com_menuMapper">

	<select id="read" resultType="Com_menuVO">
		select * from com_menu
		where menu_no = #{menu_no}
	</select>

	<insert id="create">
		insert into com_menu
		(menu_no,menu_cd,menu_nm,menu_icon,menu_url,menu_target,menu_pcd,menu_lv,menu_inx,menu_fullnm,menu_summary,menu_user,menu_use,menu_dt) values
		(#{menu_no},#{menu_cd},#{menu_nm},#{menu_icon},#{menu_url},#{menu_target},#{menu_pcd},#{menu_lv},#{menu_inx},#{menu_fullnm},#{menu_summary},#{menu_user},#{menu_use},#{menu_dt})
	</insert>

	<update id="update">
		update com_menu set
		menu_cd = #{menu_cd},
		menu_nm = #{menu_nm},
		menu_icon = #{menu_icon},
		menu_url = #{menu_url},
		menu_target = #{menu_target},
		menu_pcd = #{menu_pcd},
		menu_lv = #{menu_lv},
		menu_inx = #{menu_inx},
		menu_fullnm = #{menu_fullnm},
		menu_summary = #{menu_summary},
		menu_user = #{menu_user},
		menu_use = #{menu_use},
		menu_dt = #{menu_dt}
		where
		menu_no = #{menu_no}
	</update>

	<delete id="delete">
		delete from com_menu where menu_no = #{menu_no}
	</delete>

	<select id="listAll" resultType="Com_menuVO">
 		<![CDATA[ select * from com_menu]]>
	</select>

</mapper>